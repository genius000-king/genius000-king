// إعداد الاتصال — المكان الوحيد الذي يحمل رابط القاعدة ومفتاحها.
// ❌ لا يُعرض في أي واجهة (Spec §1.2 #6).
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';

// وضع البيانات الوهمية — يتحوّل إلى false في المهمة 28.
export const USE_MOCK = true;

// حاوية التخزين في Supabase Storage.
export const STORAGE_BUCKET = 'media';
