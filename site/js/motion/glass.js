// ============================================================
// glass.js — لم يعد يفعل شيئاً، عن قصد.
//
// كانت هذه الوحدة تبني الزجاج بنفسها: حلقة انكسار، زاوية بريق تدور
// مع التمرير، بقعة ضوء تتبع المؤشّر، ومرشّحة SVG بـ feDisplacementMap
// تشوّه الخلفية فعلياً. كان ذلك زجاجاً من صنعنا — ثقيلاً، ومختلفاً
// بين متصفّح وآخر.
//
// الزجاج الآن وصفة CSS قياسية بالكامل (القسم 7 في css/app.css):
// خلفية rgba + backdrop-filter + إطار 1px + ظلّ. لا يحتاج JavaScript.
//
// نُبقي التسجيل قائماً حتى لا يشتكي السجل من `data-fx="glass"`
// المكتوبة على البلاطات، ولتبقى إزالتها من الـ HTML تغييراً منفصلاً.
// ============================================================
export default {
  name: 'glass',
  init() { /* الزجاج كلّه في CSS */ },
  destroy() {},
};
