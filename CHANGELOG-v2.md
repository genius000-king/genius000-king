# الإصدار الثاني — الموقع ولوحة المشرف

## المظهر

- **الخلفية رمادي غامق محايد** بدل الأزرق المسوَدّ. الأزرق صار لون هوية
  لا لون خلفية، فصار الزجاج فوقه يُقرأ زجاجاً لا لوحاً ملوّناً.
- **زجاج v2 في الموقعين.** ستّ طبقات: مادة، انكسار عند الحدّ، بريق
  إطار مقنَّع، زيغ لوني، حبيبات، وسُمك. اللوحة كانت ألواحاً مصمتة بلا
  علاقة بالموقع — الآن نفس اللغة البصرية بالضبط.
- **خطّ جديد: Cairo متغيّر.** ملفّان يغطّيان الأوزان 300→900 بدل عشرين
  ملفاً ثابتاً (140KB بدل 400KB).
- **إصلاح الطباعة العربية.** الإصدار الأول كان يطبّق `letter-spacing`
  و`text-transform: uppercase` على نصّ عربي: الأول يقطع وصل الحروف
  والثاني بلا معنى أصلاً. أُزيلا، وارتفع طول السطر ليناسب النقاط
  والهمزات.

## الشعار

- **يتفاعل من أي مكان.** كان لا يستجيب إلا لمن يقف فوقه بالضبط. الآن
  الاستماع على النافذة كلها: أي سحب في أي موضع من الصفحة يحرّكه —
  قريباً كان الإصبع (نفور موضعي) أو بعيداً (ريح باتجاه السحب وسرعته).
- **التحوّل تدريجي لا مفاجئ.** كان يبدّل حالتين بـ opacity. الآن قيمة
  واحدة مستمرّة تُحسب كل إطار، ولكل جسيم دوره — فيتفكّك الشعار موجةً
  موجةً من مركزه إلى أطرافه، ويعود كذلك.
- لوحة الرسم أوسع من الشعار 26% في كل اتجاه فلا تُقصّ الجسيمات.
- نفس المحرّك يعمل في شاشة دخول اللوحة.

## اللمس والمتصفح

- إلغاء الوميض الأزرق عند الضغط على أي شيء (`-webkit-tap-highlight-color`).
- منع تحديد النصّ العشوائي على الأزرار والشعار والأيقونات، مع إبقائه
  على النصّ الحقيقي الذي يُقرأ ويُنسخ.
- منع سحب الصور وقائمة اللمس الطويل على الشعار.
- حلقة تركيز واضحة للوحة المفاتيح فقط، لا إطار عند النقر بالفأرة.

## سرعة التحميل — «حمّل مرّة واحدة»

- **ملفّ CSS واحد** لكل مشروع بدل سلسلة `@import` من ٢٦ ملفاً كان
  المتصفّح يجلبها واحداً بعد الآخر قبل أن يرسم أي شيء.
- **ذاكرة على الجهاز:** الزيارة الثانية تُرسم كاملة في الإطار الأول من
  نسخة محفوظة، والشبكة تُسأل بعدها بصمت — ولا تُعاد الرسمة إلا إن تغيّر
  شيء فعلاً.
- **عامل خدمة (Service Worker):** الملفّات والخطوط والصور تُحفظ على
  القرص عند أول زيارة، فتفتح المرّات التالية بلا شبكة.
- `modulepreload` و`preconnect` لتسطيح شلال الطلبات في أول زيارة.
- ❌ لا تخزين لطلبات قاعدة البيانات في عامل الخدمة عمداً: تعديلات اللوحة
  يجب أن تظهر فوراً.

## لوحة المشرف على الجوال

| ما كان يحدث | السبب |
|---|---|
| الشريط الجانبي والدرج يظهران فوق المحتوى وهما «مغلقان» | في RTL يعني `inset-inline-end` الحافّة اليسرى بينما `translate` يبقى فيزيائياً، فكان الإزاحة تدخلهما الشاشة بدل إخراجهما |
| مؤشّر القسم النشط مختفٍ | `-var(--a-3)` صيغة غير صحيحة تُسقط السطر كلّه |
| التنبيهات لا تُقرأ | كانت خلف شريط التبويب السفلي |
| الصفحة تكبر عند الكتابة في أي حقل | خطّ الحقول 13.5px، وiOS يكبّر تلقائياً تحت 16px |
| شبح السحب يقفز للجهة المقابلة | `inset-inline-start` مع إحداثيات `getBoundingClientRect` الفيزيائية |
| المحتوى يلامس حواف الشاشات المنحنية | بلا `env(safe-area-inset-*)` |
| أزرار القسم تفيض أفقياً | `margin-inline-start: auto` على شاشة ضيّقة |

إضافةً إلى: الدرج يملأ عرض الشاشة، والمودال لا يتجاوز ارتفاعها، ومناطق
اللمس ≥ 44px، وشريط تبويب سفلي أوضح، ومقاسات أصغر تحت 400px.

## الحماية

- قفل كلمة دخول قبل شاشة تسجيل الدخول في اللوحة
  (`admin/js/core/gate.js` — سطر واحد لتغيير الكلمة).
- اللوحة تبقى `noindex, nofollow`.
- حمايتك الحقيقية تبقى حساب Supabase وسياسات RLS.
